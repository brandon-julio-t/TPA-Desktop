<Window x:Class="TPA_Desktop.Views.HumanResource.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:TPA_Desktop.Views.HumanResource"
        xmlns:ns0="using:TPA_Desktop.Views.HumanResource"
        mc:Ignorable="d"
        Title="Human Resource"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        d:DataContext="{d:DesignInstance}">
    <StackPanel Margin="20"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
        <TextBlock Text="{Binding Name, StringFormat=Hello\, {0}.}" />
        <TextBlock Text="{Binding Role, StringFormat=Logged in as {0}.}" />
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
            <Button Click="HandleNewEmployee">New employee</Button>
            <Button Click="HandleNewViolation">New violation</Button>
        </StackPanel>
    </StackPanel>
</Window>