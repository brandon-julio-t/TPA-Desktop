<Window x:Class="TPA_Desktop.Views.CustomerService.CustomerServiceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:TPA_Desktop.Views.CustomerService"
        xmlns:userControls="clr-namespace:TPA_Desktop.Views.UserControls"
        mc:Ignorable="d"
        Title="Customer Service"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        d:DataContext="{d:DesignInstance local:CustomerServiceWindowViewModel}">
    <Grid Margin="20" Width="500">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <userControls:Greeting Grid.Row="0" />
        
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBlock Text="Current queue number: " />
            <TextBlock Text="{Binding CurrentQueue.Number}" />
        </StackPanel>
        
        <Button Grid.Row="2" Content="Next Queue" Click="HandleNextCustomerQueue" />

        <StackPanel Grid.Row="3">
            <TextBlock Text="Account" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="New Individual Account" Click="HandleNewIndividualAccount" />
                <Button Grid.Column="1" Content="New Business Account" />
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="4">
            <TextBlock Text="Customer" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="New Customer" Click="HandleRegisterNewCustomer" />
                <Button Grid.Column="1" Content="Update Customer Data" />
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="5">
            <TextBlock Text="Virtual Account" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="Create Virtual Account" Click="CreateVirtualAccount" />
                <Button Grid.Column="1" Content="Generate Virtual Accounts From Excel" Click="GenerateVirtualAccountsFromExcel" />
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="6">
            <TextBlock Text="Payroll" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="New Payroll" />
                <Button Grid.Column="1" Content="Generate Payrolls From Excel" />
            </Grid>
        </StackPanel>
    </Grid>
</Window>